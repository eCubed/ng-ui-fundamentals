import { OverlayableDirective } from './overlayable.directive';

describe('OverlayableDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayableDirective();
    expect(directive).toBeTruthy();
  });
});
