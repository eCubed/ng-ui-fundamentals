import { DraggableAreaDirective } from './draggable-area.directive';

describe('DraggableAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableAreaDirective();
    expect(directive).toBeTruthy();
  });
});
